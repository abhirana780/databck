// Production Configuration for BI Portal
const CONFIG = {
    // Change this to your deployed backend URL (e.g., https://your-backend.herokuapp.com)
    // Keep it empty or "http://127.0.0.1:5000" for local development
    API_BASE_URL: "https://databck-x0jn.onrender.com" || "http://127.0.0.1:5000" 
};

console.log("Portal Configuration Loaded:", CONFIG);
